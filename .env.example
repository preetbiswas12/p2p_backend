# P2P Backend Configuration

# Server Ports
PORT=3000
SIGNALING_PORT=3001

# Environment
NODE_ENV=development

# Memory Protection (cap heap ~256MB and allow GC)
NODE_OPTIONS=--max-old-space-size=256 --expose-gc

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,https://yourdomain.com

# Logging
LOG_LEVEL=info

# Room Configuration
ROOM_INACTIVITY_TIMEOUT=10800000
PEER_HEARTBEAT_TIMEOUT=300000
CLEANUP_CHECK_INTERVAL=60000

# Memory Thresholds (in MB)
MEMORY_WARNING_THRESHOLD=200
MEMORY_CRITICAL_THRESHOLD=250

# Server Mode
COLLAB_MODE=p2p
